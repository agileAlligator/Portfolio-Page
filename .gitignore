# Jekyll build output & cache
_site/
.jekyll-cache/
.jekyll-metadata
.sass-cache/

# Unpublished drafts — kept local, never pushed
_drafts/

# Ruby / gems
vendor/
Gemfile.lock
*.gem

# Env / secrets (belt-and-suspenders — none live in this repo)
.env
*.key

# OS noise
.DS_Store

# OG image generator — cached fonts (re-downloaded on first run)
scripts/.fonts/
